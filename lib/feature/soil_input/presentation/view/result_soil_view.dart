import 'package:flutter/material.dart';

class ResultSoilView extends StatelessWidget {
  const ResultSoilView({super.key, required data});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Result Soil View')));
  }
}
